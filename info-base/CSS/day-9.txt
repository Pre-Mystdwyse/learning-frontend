имеет смысл выносить какие-либо модалки после main в конце body, чтобы
непосредственно z-index был больше других элементов;

если задать переменную в html посредством style, то такая переменная
будет иметь неограниченную обасть видимости по дочерним элементам, но
выше самого тега, где была объявлена переменная, использовать её невозможно;

у переменных, заданных в тегах приоритет выше, чем у, например, заданных
в root;

